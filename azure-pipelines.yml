trigger:
  branches:
    include:
      - main
      - dev

pr:
  branches:
    include:
      - main

pool:
  name: 'local-agent'

stages:
  - stage: CI
    displayName: 'Build & Test'
    jobs:
      - job: Frontend
        displayName: 'Frontend (Vite)'
        steps:
          - checkout: self

          - task: NodeTool@0
            inputs:
              versionSpec: '20.x'
            displayName: 'Use Node 20'

          - script: |
              node --version
              npm --version
            displayName: 'Verify Node & npm'

          - script: npm ci
            displayName: 'Install dependencies'

          - script: npm run build
            displayName: 'Build (Vite)'

          - publish: dist
            artifact: frontend-dist
            displayName: 'Publish build artifact'

  - stage: CD
    displayName: 'Deploy to Azure'
    dependsOn: CI
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - deployment: DeployFrontend
        displayName: 'Deploy to App Service'
        environment: 'production'
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: frontend-dist
                  displayName: 'Download build artifact'

                - task: PowerShell@2
                  displayName: 'Write publish profile to file'
                  inputs:
                    targetType: 'inline'
                    script: |
                      $profile = '$(AZURE_WEBAPP_PUBLISH_PROFILE)'
                      Set-Content -Path "$(Pipeline.Workspace)/publish.pubxml" -Value $profile

                - task: AzureRmWebAppDeployment@4
                  displayName: 'Deploy to jaishanidev-app'
                  inputs:
                    ConnectionType: 'PublishProfile'
                    PublishProfilePath: '$(Pipeline.Workspace)/publish.pubxml'
                    appName: 'jaishanidev-app'
                    packageForZip: '$(Pipeline.Workspace)/frontend-dist'